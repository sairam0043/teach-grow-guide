# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files — every variant, anywhere in the tree.
# .env.example is deliberately tracked as the documented template.
.env
.env.*
!.env.example


# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# NOTE: patterns must not start with "./" — git treats those as literal and
# they match nothing. The rules below are anchored to this file's directory.
backend/node_modules
backend/dist
backend/dist-ssr
# backend/uploads
backend/uploads/
backend/scratch/
test-results/